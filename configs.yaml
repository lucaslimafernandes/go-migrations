postgres:
  APPLY: true
  HOST: localhost
  PORT: 5432
  USER: postgres
  PASSWORD: password

mysql:
  APPLY: false
  HOST: localhost
  PORT: 3306
  USER: myuser
  PASSWORD: user_password
  DBNAME: mydb
  ROOT_PASSWORD: root_password